{
  "name": "pi-transcribe",
  "version": "0.1.0",
  "type": "module",
  "description": "Speech-to-text dictation extension for pi coding agent — hold spacebar, speak, release. All transcription runs locally.",
  "license": "MIT",
  "keywords": ["pi-package", "speech-to-text", "dictation", "transcription", "voice"],
  "repository": {
    "type": "git",
    "url": "https://github.com/georgebashi/pi-transcribe"
  },
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@picovoice/pvrecorder-node": "^1.2.8"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
